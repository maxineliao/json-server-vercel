{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "description": "Deploy JSON Server to Vercel",
  "main": "server.js",
  "scripts": {
    "start": "curl -s -o db.json https://raw.githubusercontent.com/maxineliao/json-server-vercel/main/db.json && json-server --watch db.json"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.0",
    "json-server-auth": "^2.1.0"
  }
}
